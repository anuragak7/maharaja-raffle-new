services:
  - type: web
    name: maharaja-raffle
    env: node
    plan: free
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: ./scripts/render-setup.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        generateValue: true