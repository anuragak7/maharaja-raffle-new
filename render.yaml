services:
  - type: web
    name: maharaja-raffle
    env: node
    plan: free
    buildCommand: npm install && npx prisma generate && npx prisma db push --force-reset && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        generateValue: true